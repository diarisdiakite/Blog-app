<%= form_with(model: [@current_user, @post], url: user_posts_path(@current_user), method: :post) do |form| %>
  <%= form.text_field :title %>
  <%= form.text_area :text %>
  <%= form.submit "Create new post" %>
<% end %>
